на вход подается ступенчатый массив в котором кол-во элементов в каждой строке - разная
ввод каждой каждой строки массива выполнить с помощью функции

1) опр номера строк массива в котором имеется элемент, значение которого > суммы всех элементов строки
2) опр для каждой строки наименьшую длину подпоследовательности, состоящую из равномерно убывающих элементов (под последовательность с двойки)